# ===========================================================================
# retrieval-failure-mechanism - pinned runtime stack.
#
# These pins MIRROR the stack recorded in the prior repos result artifacts'
# `environment` block (torch 2.11.0+cu128, transformers 4.47.0, numpy 2.0.2,
# scipy 1.16.3), so a value patched/captured here is bit-compatible with the
# detection artifacts it consumes. The GPU experiments run on Colab (A100/L4);
# reproduce that stack there. Torch is intentionally left to the Colab/CUDA
# wheel index (install torch separately, matched to the CUDA runtime) - see
# README "Reproduction".
#
# CPU-only local dev (tests that need no model) works with any recent torch;
# the numbers in results/ must come from the pinned GPU stack below.
# ===========================================================================

# --- core numerics / config ---
numpy==2.0.2
scipy==1.16.3
pyyaml==6.0.2

# --- model stack (install a CUDA-matched torch wheel first; see README) ---
# torch==2.11.0            # install via the CUDA wheel index on Colab
transformers==4.47.0
accelerate==1.13.0
bitsandbytes==0.49.2       # 8-bit / NF4 loads for the pinned rings
huggingface_hub==0.36.2
safetensors>=0.4.5
sentencepiece>=0.2.0       # gemma / llama tokenizers
tokenizers>=0.20,<0.21

# --- testing ---
pytest==9.0.3
